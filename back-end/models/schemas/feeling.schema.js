export default class Feeling {
    // Constructor để khởi tạo đối tượng
    constructor({ quote, author, department, avatar }) {

        this.quote = quote;
        this.author = author;
        this.department = department;
        this.avatar = avatar;
    }
}