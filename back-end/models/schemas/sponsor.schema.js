export default class Sponsor {
    // Constructor để khởi tạo đối tượng
    constructor({ name, logo, kind, competition_id }) {

        this.name = name;
        this.logo = logo;
        this.kind = kind;
        this.competition_id = competition_id;
    }
}