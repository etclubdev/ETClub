

export default class Banner {
    // Constructor để khởi tạo đối tượng
    constructor({ description, img, link }) {

        this.description = description;
        this.img = img;
        this.link = link;
    }
}